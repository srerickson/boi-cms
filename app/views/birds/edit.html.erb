<div class="span-24 append-bottom">
  <div class="bird_title"><%= @bird.name %></div>

  <%= link_to "delete this bird", bird_path(@bird), :method => :delete, :confirm => "Are you sure you want to delete the project? This cannot be un-done.", :style=>"float:right;"%>
</div>

<%= render :partial => "form" %>
