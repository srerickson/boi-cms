<% assets.each_with_index do |ass, i| %>

  <%= image_tag ass.asset.url("sq50") %>
  <%= link_to  bird_asset_path(attached_to, ass) , :method => :delete, :remote=> true, :confirm => "Are you sure you want to delete the image?", :alt => "delete"  do %>
    <img src="/images/red-x.png" style="position:relative;top:-28px;margin-right:10px;"/>
  <% end %>
<% end %>
